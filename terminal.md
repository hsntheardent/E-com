# check all files
ls -a

# making folder .git
git init


# making file .gitignore
then paste file from url "gitignore.io"

# upload files on git
git add -A

# for first time
git config --global user.name "Ehtesham Hassan"
git config --global user.email "ehteshamhassan353@gmail.com"

# 
git commit -m "my first commit"

# 
git remote set-url origin git@github.com:hsntheardent/E-com.git

#
git branch -M main

#
git push -u origin m
 

# new code add kia
git status
# push then on github
git add -A
git commit -m "Describe your changes"
git push origin main
git push

###