# Context_procssor.py
settings.py → menu_links ko har template ke liye enable karta hai
menu_links() → DB se sari categories fetch karta hai 
return {'links': links} → categories HTML ko send karta hai
navbar.html → categories ko menu me show karta hai
category.get_url → har category ka URL banata hai
get_url() → slug se dynamic link generate karta hai
User click → us category ke products open ho jate hain
                                                            Context processor DB se categories uthata hai → templates ko deta hai → navbar unko show karta hai → har category ka URL slug se banta hai → click par us category ke products open ho jate hain.



 ok np